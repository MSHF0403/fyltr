# fyltime

ファイルを更新時間で絞り込むシンプルなlsを拡張したCLIツール

## Overview
fyltimeは、ディレクトリ内のファイルを走査し、更新日時に基づいてファイルを絞り込む軽量なCLIツールです。  
複雑なコマンドを使用せず、直感的に古いファイルや最近更新されたファイルを抽出することを目的としています。  
ファイル整理や不要ファイルの発見を効率化するために設計されています。

## Usage
```text
Usage:
  fyltime [OPTION]

OPTION:
```text
  --since <期間>         指定した期間以内に更新されたファイルを表示（例: 3d, 12h）
  --after <日時>         指定した日時以降に更新されたファイルを表示（例: 2024-01-01）
  --before <日時>        指定した日時以前に更新されたファイルを表示
  --ext <拡張子>         指定した拡張子のファイルのみ表示（例: rs, txt）
  -a, --all              隠しファイルを含めて表示
  -h, --help             ヘルプを表示
  -v, --version          現在のバージョンを表示
```
## About

fyltimeは、ファイルの更新時間に基づいて一覧表示および絞り込みを行うCLIツールである。  
既存のコマンドであるlsやfindでも同様の操作は可能であるが、時間条件を指定した検索はオプションが複雑になりやすい。  
本ツールはそれらを簡潔に扱えるようにすることを目的として開発された。

特に、「一定期間更新されていないファイルの抽出」や「最近更新されたファイルの確認」といった用途において、直感的な操作で実行できる点を特徴としている。  
また、lsの基本的な一覧表示機能を持ちつつ、時間によるフィルタリングに特化したツールとして位置付けられる。

---

### 開発者
MSHF0403

### ライセンス
GNU General Public License v2.0

### 名前の由来
「file」と「time」を組み合わせた造語であり、ファイルを時間で扱うツールであることを表している。

### バージョン履歴
v0.1.0：初期バージョン
